export const sendWelcomeEmail = async () => {
    throw new Error('Email workflows were removed from the simplified OpenStock build.');
};

export const sendNewsSummaryEmail = async () => {
    throw new Error('Email workflows were removed from the simplified OpenStock build.');
};
